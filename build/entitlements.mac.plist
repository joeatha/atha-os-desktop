<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Microphone access for the softphone -->
  <key>com.apple.security.device.audio-input</key>
  <true/>

  <!-- Camera access for video meetings (e.g. Google Meet) opened in-app -->
  <key>com.apple.security.device.camera</key>
  <true/>

  <!-- Electron under the hardened runtime needs these JIT/memory entitlements -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Allow loading of embedded frameworks/helpers -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Passkeys: where the Secure Enclave WebAuthn credentials are stored. MUST match
       KEYCHAIN_ACCESS_GROUP in src/webauthn.js — app.configureWebAuthn silently
       fails to produce a working authenticator if the two disagree. -->
  <key>keychain-access-groups</key>
  <array>
    <string>YMHAWA25ZZ.com.athaenterprises.athaos.webauthn</string>
  </array>
</dict>
</plist>
